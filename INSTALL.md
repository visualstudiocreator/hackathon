# 📦 Инструкция по установке

## Системные требования

### Минимальные требования:
- **ОС:** Windows 10/11, Linux (Ubuntu 20.04+), macOS 10.15+
- **Python:** 3.8 или выше
- **RAM:** 8 GB (рекомендуется 16 GB)
- **Место на диске:** 10 GB (для моделей и зависимостей)
- **VRAM:** До 80 GB для всех NLP-моделей

### Рекомендуемые требования:
- **Python:** 3.10+
- **RAM:** 16 GB
- **CPU:** 4+ ядра
- **GPU:** Опционально, для ускорения обработки

## Установка по шагам

### Шаг 1: Установка Python

#### Windows
1. Скачайте Python с [python.org](https://www.python.org/downloads/)
2. Запустите установщик
3. ✅ **Важно:** Отметьте "Add Python to PATH"
4. Нажмите "Install Now"

Проверка установки:
```cmd
python --version
```

#### Linux (Ubuntu/Debian)
```bash
sudo apt update
sudo apt install python3 python3-pip python3-venv
```

Проверка:
```bash
python3 --version
pip3 --version
```

#### macOS
```bash
# Используйте Homebrew
brew install python@3.10
```

### Шаг 2: Загрузка проекта

#### Вариант A: Клонирование репозитория (если доступно)
```bash
git clone <repository-url>
cd screenplay-analyzer
```

#### Вариант B: Распаковка архива
1. Распакуйте архив в удобную папку
2. Откройте терминал/командную строку в этой папке

### Шаг 3: Создание виртуального окружения (рекомендуется)

#### Windows
```cmd
python -m venv venv
venv\Scripts\activate
```

#### Linux/macOS
```bash
python3 -m venv venv
source venv/bin/activate
```

После активации вы увидите `(venv)` в начале строки.

### Шаг 4: Установка зависимостей

```bash
# Обновление pip
pip install --upgrade pip

# Установка всех зависимостей
pip install -r requirements.txt
```

**Примечание:** Первая установка может занять 5-10 минут, так как загружаются NLP-модели.

#### Если возникают ошибки при установке:

##### Ошибка с `torch`:
```bash
# Для CPU версии
pip install torch --index-url https://download.pytorch.org/whl/cpu

# Для CUDA (если есть GPU)
pip install torch --index-url https://download.pytorch.org/whl/cu118
```

##### Ошибка с `natasha`:
```bash
pip install natasha==1.6.0 --no-cache-dir
```

##### Ошибка с `PyPDF2`:
```bash
pip install PyPDF2==3.0.1 pdfplumber==0.10.3
```

### Шаг 5: Проверка установки

Запустите тестовый скрипт:

```bash
python -c "import fastapi, natasha, torch; print('Все зависимости установлены!')"
```

Если команда выполнилась без ошибок - всё готово!

### Шаг 6: Первый запуск

#### Windows
```cmd
python app.py
```

или используйте готовый скрипт:
```cmd
run.bat
```

#### Linux/macOS
```bash
python3 app.py
```

или:
```bash
chmod +x run.sh
./run.sh
```

После запуска откройте браузер: `http://localhost:8000`

## Альтернативные методы установки

### Использование Docker (планируется)

```dockerfile
# Будет добавлено в будущих версиях
```

### Установка только основных компонентов

Если у вас ограничены ресурсы, можно установить минимальный набор:

```bash
pip install fastapi uvicorn python-multipart
pip install PyPDF2 python-docx chardet
pip install pandas openpyxl
pip install natasha razdel pymorphy3
```

## Настройка для разработки

Если вы планируете модифицировать код:

```bash
# Установка дополнительных инструментов
pip install black flake8 pytest ipython jupyter

# Форматирование кода
black *.py

# Проверка стиля
flake8 *.py
```

## Обновление зависимостей

Для обновления всех зависимостей до последних версий:

```bash
pip install --upgrade -r requirements.txt
```

## Решение проблем

### Проблема: "Python не найден"
**Решение:**
- Убедитесь, что Python добавлен в PATH
- Попробуйте `python3` вместо `python`
- Переустановите Python с галочкой "Add to PATH"

### Проблема: "pip не найден"
**Решение:**
```bash
python -m ensurepip --upgrade
```

### Проблема: "Не хватает памяти при установке"
**Решение:**
```bash
pip install --no-cache-dir -r requirements.txt
```

### Проблема: "ModuleNotFoundError"
**Решение:**
```bash
# Проверьте активацию виртуального окружения
# Переустановите конкретный модуль
pip install --upgrade <module-name>
```

### Проблема: "Порт 8000 уже занят"
**Решение:**

Измените порт в `app.py`:
```python
uvicorn.run("app:app", host="0.0.0.0", port=8080)  # Используем 8080
```

Или убейте процесс на порту 8000:
```bash
# Windows
netstat -ano | findstr :8000
taskkill /PID <PID> /F

# Linux/macOS
lsof -ti:8000 | xargs kill -9
```

### Проблема: "Медленная работа"
**Решения:**
1. Используйте более мощный процессор
2. Увеличьте RAM
3. Уменьшите размер файлов
4. Измените пресет на 'basic'

## Проверка системы

Запустите проверку всех компонентов:

```python
python -c "
import sys
print('Python:', sys.version)

try:
    import fastapi
    print('✓ FastAPI')
except: print('✗ FastAPI')

try:
    import natasha
    print('✓ Natasha')
except: print('✗ Natasha')

try:
    import torch
    print('✓ PyTorch')
except: print('✗ PyTorch')

try:
    import pandas
    print('✓ Pandas')
except: print('✗ Pandas')

print('Установка завершена!')
"
```

## Деинсталляция

Для полного удаления:

```bash
# Деактивация виртуального окружения
deactivate

# Удаление виртуального окружения
rm -rf venv/  # Linux/macOS
rmdir /s venv  # Windows

# Удаление загруженных моделей и файлов
rm -rf models/ uploads/ outputs/
```

## Следующие шаги

После успешной установки:

1. Прочитайте [QUICKSTART.md](QUICKSTART.md) для быстрого старта
2. Изучите [README.md](README.md) для полной документации
3. Попробуйте примеры из [api_examples.py](api_examples.py)
4. Откройте `http://localhost:8000/docs` для API документации

## Поддержка

Если проблема не решается:

1. Проверьте системные требования
2. Убедитесь, что все команды выполнялись последовательно
3. Проверьте логи в консоли
4. Попробуйте переустановить в чистое виртуальное окружение

---

**Важно:** Сохраняйте логи ошибок - они помогут в диагностике проблем.

