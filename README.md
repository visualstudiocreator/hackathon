# 🎬 Препродакшн Анализатор Сценариев

Автоматизированный сервис для подготовки препродакшн-документации на основе текстовых сценариев на русском языке.

## 📋 Описание

Сервис автоматически обрабатывает сценарии в форматах PDF и DOCX, извлекая производственные элементы и создавая структурированные таблицы для препродакшн-планирования. Система использует NLP-анализ для определения локаций, персонажей, реквизита, спецэффектов и других элементов съемки.

## ✨ Возможности

- **Автоматическое распознавание сцен** - система определяет номера сцен и сегментирует текст
- **Извлечение производственных элементов**:
  - Локации и время суток
  - Интерьер/экстерьер
  - Основные и второстепенные персонажи
  - Массовка
  - Реквизит
  - Транспортные средства
  - Животные
  - Спецэффекты
  - Грим и костюмы
  - Каскадерские трюки
  - Музыка и звуковые эффекты
  - Оружие
  - Еда и напитки
- **Гибкие пресеты анализа** - базовый, расширенный и полный
- **Настраиваемые колонки** - возможность выбора нужных категорий
- **Экспорт в CSV и XLSX** с форматированием
- **Сводная статистика** - анализ персонажей, локаций и других элементов
- **Веб-интерфейс** - удобный интерфейс для загрузки и обработки

## 🚀 Установка

### Требования

- Python 3.8+
- 80 GB VRAM (для загрузки NLP-моделей)

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Создание необходимых директорий

Директории создаются автоматически при запуске, но можно создать вручную:

```bash
mkdir uploads outputs models static templates
```

## 💻 Использование

### Веб-интерфейс

Запустите веб-сервер:

```bash
python app.py
```

Откройте в браузере: `http://localhost:8000`

#### Интерфейс позволяет:

1. Загрузить файл сценария (перетаскиванием или выбором)
2. Выбрать пресет анализа
3. Выбрать формат экспорта (XLSX или CSV)
4. Получить готовые таблицы и статистику

### Программное использование

```python
from screenplay_processor import ScreenplayProcessor

# Создаем процессор
processor = ScreenplayProcessor()

# Обрабатываем сценарий
result = processor.process_screenplay(
    file_path='screenplay.pdf',
    preset='extended',
    export_format='xlsx'
)

if result['success']:
    print(f"Обработано {result['scenes_count']} сцен")
    print(f"Результат: {result['output_file']}")
else:
    print(f"Ошибка: {result['error']}")
```

## 📊 Пресеты анализа

### Базовый (basic)
Минимальный набор данных для быстрого анализа:
- Номер сцены
- Локация
- Время суток
- Персонажи
- Краткое описание

### Расширенный (extended)
Оптимальный набор для большинства проектов:
- Номер сцены
- Локация
- Внутри/Снаружи
- Время суток
- Персонажи (основные)
- Персонажи (второстепенные)
- Массовка
- Реквизит
- Краткое описание

### Полный (full)
Максимально детальный анализ:
- Все колонки расширенного пресета
- Транспорт
- Животные
- Спецэффекты
- Грим/Костюмы
- Каскадеры
- Музыка/Звук
- Оружие
- Еда/Напитки

## 🔧 Конфигурация

Основные настройки находятся в `config.py`:

```python
# Ограничения
MAX_FILE_SIZE_MB = 50  # Максимальный размер файла
MAX_PAGES = 120        # Максимальное количество страниц
MAX_PROCESSING_TIME_MINUTES = 5  # Максимальное время обработки

# Поддерживаемые кодировки
SUPPORTED_ENCODINGS = [
    'utf-8', 'utf-16', 'cp1251', 'windows-1251', 
    'koi8-r', 'iso-8859-5', 'macroman', 'ascii'
]
```

## 📁 Структура проекта

```
.
├── app.py                    # Веб-приложение FastAPI
├── screenplay_processor.py   # Основной процессор
├── document_parser.py        # Парсинг PDF/DOCX
├── scene_segmenter.py        # Сегментация на сцены
├── nlp_analyzer.py           # NLP-анализ текста
├── data_exporter.py          # Экспорт данных
├── config.py                 # Конфигурация
├── requirements.txt          # Зависимости
├── templates/
│   └── index.html           # HTML-интерфейс
├── uploads/                 # Загруженные файлы
├── outputs/                 # Результаты обработки
└── README.md
```

## 🎯 API Endpoints

### POST `/api/upload`
Загрузка и обработка сценария

**Параметры:**
- `file` - файл сценария (PDF/DOCX)
- `preset` - пресет анализа (basic/extended/full)
- `custom_columns` - пользовательские колонки (JSON)
- `export_format` - формат экспорта (xlsx/csv)

**Ответ:**
```json
{
  "success": true,
  "scenes_count": 45,
  "pages": 98,
  "processing_time": 45.2,
  "output_file": "screenplay_20231118_143022.xlsx",
  "summary_file": "screenplay_20231118_143022_summary.xlsx",
  "download_url": "/api/download/screenplay_20231118_143022.xlsx"
}
```

### GET `/api/download/{filename}`
Скачивание обработанного файла

### POST `/api/preview`
Превью первых сцен без полной обработки

### GET `/api/presets`
Получение списка доступных пресетов

### GET `/api/columns`
Получение списка всех доступных колонок

### GET `/health`
Проверка работоспособности сервиса

## 🧠 NLP-модели

Система использует следующие компоненты для анализа русского языка:

- **Natasha** - извлечение именованных сущностей (NER)
- **PyMorphy3** - морфологический анализ
- **Razdel** - сегментация текста

Все модели работают локально без обращения к внешним API.

## 📝 Поддерживаемые форматы

### Входные форматы:
- PDF (текстовый)
- DOCX

### Выходные форматы:
- XLSX (Excel с форматированием)
- CSV (с UTF-8 BOM)

## ⚙️ Технические ограничения

- Максимальный размер файла: 50 МБ
- Максимальное количество страниц: 120
- Максимальное время обработки: 5 минут
- Размер моделей: до 80 GB VRAM

## 🎨 Формат сценария

Система лучше всего работает со стандартизированными сценариями, где:

- Сцены начинаются с номера и описания локации
- Используется формат: `ИНТЕРЬЕР/ЭКСТЕРЬЕР. ЛОКАЦИЯ - ВРЕМЯ`
- Имена персонажей в диалогах пишутся заглавными буквами
- Описания действий четко структурированы

**Пример:**

```
1. ИНТ. КУХНЯ - ДЕНЬ

Просторная современная кухня. За столом сидит ИВАН (35), 
читает газету и пьет кофе. На столе лежит пистолет.

ИВАН
Сегодня будет долгий день.

Входит МАРИЯ (32) с букетом цветов.
```

## 🤝 Вклад в проект

Проект открыт для улучшений:

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Отправьте pull request

## 📄 Лицензия

MIT License

## 👥 Авторы

Разработано для автоматизации препродакшн-процессов в кинопроизводстве.

## 🐛 Известные ограничения

- Точность распознавания зависит от качества форматирования сценария
- Рукописные PDF не поддерживаются (только текстовые)
- Система оптимизирована для русского языка

## 📞 Поддержка

При возникновении проблем проверьте:

1. Формат файла соответствует требованиям
2. Сценарий имеет четкую структуру
3. Размер файла не превышает ограничений
4. Все зависимости установлены

## 🔮 Планы развития

- [ ] Поддержка англоязычных сценариев
- [ ] Интеграция с системами планирования съемок
- [ ] Визуализация календарного плана
- [ ] Автоматическое составление call-sheets
- [ ] Оценка бюджета на основе элементов
- [ ] Экспорт в форматы специализированного ПО
- [ ] API для интеграции с другими системами

---

**Примечание:** Система является вспомогательным инструментом и не заменяет профессиональную работу продюсеров и ассистентов режиссеров. Рекомендуется проверять и корректировать результаты анализа.

