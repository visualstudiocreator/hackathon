# 🚀 Быстрый старт

## Установка и запуск за 3 шага

### Шаг 1: Установите зависимости

```bash
pip install -r requirements.txt
```

> **Примечание:** При первом запуске модели Natasha и другие NLP-компоненты будут загружены автоматически. Это может занять несколько минут.

### Шаг 2: Запустите сервер

```bash
python app.py
```

### Шаг 3: Откройте браузер

Перейдите по адресу: [http://localhost:8000](http://localhost:8000)

## 📝 Первый запуск

1. **Загрузите сценарий**
   - Перетащите файл в область загрузки
   - Или нажмите на неё и выберите файл
   - Поддерживаются форматы: PDF, DOCX

2. **Выберите настройки**
   - **Пресет**: Расширенный (рекомендуется)
   - **Формат**: XLSX (для Excel)

3. **Нажмите "Обработать сценарий"**
   - Дождитесь завершения обработки
   - Скачайте результат

## 🧪 Тестирование

В проекте есть пример сценария для тестирования:

```bash
# Просмотрите пример
cat example_screenplay.txt

# Для создания тестового DOCX используйте любой текстовый редактор:
# 1. Откройте example_screenplay.txt
# 2. Сохраните как test_screenplay.docx
```

## 💡 Примеры использования

### Через веб-интерфейс (рекомендуется)

Самый простой способ - использовать веб-интерфейс:

1. Запустите `python app.py`
2. Откройте http://localhost:8000
3. Загрузите файл и получите результат

### Через Python API

```python
from screenplay_processor import ScreenplayProcessor

# Создаем процессор
processor = ScreenplayProcessor()

# Обрабатываем сценарий
result = processor.process_screenplay(
    file_path='my_screenplay.pdf',
    preset='extended',
    export_format='xlsx'
)

# Проверяем результат
if result['success']:
    print(f"✓ Обработано {result['scenes_count']} сцен")
    print(f"✓ Файл сохранен: {result['output_file']}")
else:
    print(f"✗ Ошибка: {result['error']}")
```

### Получение превью без полной обработки

```python
from screenplay_processor import ScreenplayProcessor

processor = ScreenplayProcessor()

# Быстрое превью первых 3 сцен
preview = processor.get_scenes_preview('screenplay.pdf', num_scenes=3)

if preview['success']:
    print(f"Всего сцен: {preview['total_scenes']}")
    print(f"Страниц: {preview['pages']}")
    
    for scene in preview['preview_scenes']:
        print(f"\nСцена {scene['scene_number']}: {scene['header']}")
```

### Настройка пользовательских колонок

```python
processor = ScreenplayProcessor()

# Создаем свой набор колонок
custom_columns = [
    'Номер сцены',
    'Локация',
    'Персонажи',
    'Транспорт',
    'Оружие',
    'Спецэффекты'
]

# Валидируем колонки
is_valid, error = processor.validate_custom_columns(custom_columns)

if is_valid:
    result = processor.process_screenplay(
        file_path='screenplay.pdf',
        custom_columns=custom_columns,
        export_format='xlsx'
    )
```

## 🔧 Решение проблем

### Ошибка: "Не удалось загрузить модели"

```bash
# Убедитесь, что у вас достаточно памяти
# Модели занимают несколько GB

# Попробуйте переустановить зависимости
pip install --upgrade natasha razdel pymorphy3
```

### Ошибка: "Файл слишком большой"

- Максимальный размер: 50 МБ
- Максимальное количество страниц: 120
- Измените лимиты в `config.py` если необходимо

### Ошибка: "Не удалось распознать сцены"

Убедитесь, что сценарий имеет правильный формат:

```
1. ИНТ. ЛОКАЦИЯ - ВРЕМЯ

Описание действия...

ПЕРСОНАЖ
Диалог...
```

### Медленная обработка

- Первая обработка всегда медленнее (загружаются модели)
- Для больших сценариев (100+ страниц) обработка может занять 3-5 минут
- Проверьте нагрузку на CPU/GPU

## 📊 Пресеты

### 🥉 Базовый (basic)
Для быстрого обзора - только ключевая информация

### 🥈 Расширенный (extended) ⭐
Рекомендуется для большинства проектов - оптимальный баланс

### 🥇 Полный (full)
Максимально детальный анализ - все доступные категории

## 🎯 Следующие шаги

1. **Изучите результаты**
   - Откройте сгенерированную таблицу в Excel
   - Проверьте сводную статистику

2. **Настройте под свои нужды**
   - Создайте свой пресет в `config.py`
   - Добавьте дополнительные категории

3. **Интегрируйте в рабочий процесс**
   - Используйте API для автоматизации
   - Создайте скрипты для пакетной обработки

## 📚 Дополнительные ресурсы

- [README.md](README.md) - Полная документация
- [config.py](config.py) - Настройки и пресеты
- API документация: http://localhost:8000/docs (после запуска сервера)

## 💬 Поддержка

Если возникли проблемы:

1. Проверьте, что все зависимости установлены
2. Убедитесь, что файл имеет правильный формат
3. Проверьте логи в консоли
4. Попробуйте с примером сценария

---

**Совет:** Начните с тестирования на примере сценария (`example_screenplay.txt`), чтобы убедиться, что всё работает правильно.

