# 🤝 Руководство по внесению вклада

Спасибо за интерес к проекту! Мы рады любому вкладу - от исправления опечаток до добавления новых функций.

## 📋 Содержание

- [Как начать](#как-начать)
- [Структура проекта](#структура-проекта)
- [Стиль кода](#стиль-кода)
- [Тестирование](#тестирование)
- [Отправка изменений](#отправка-изменений)
- [Приоритетные направления](#приоритетные-направления)

## Как начать

1. **Форкните репозиторий**
   ```bash
   git clone <your-fork-url>
   cd screenplay-analyzer
   ```

2. **Создайте виртуальное окружение**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/macOS
   venv\Scripts\activate     # Windows
   ```

3. **Установите зависимости для разработки**
   ```bash
   pip install -r requirements.txt
   pip install black flake8 pytest
   ```

4. **Создайте ветку для изменений**
   ```bash
   git checkout -b feature/your-feature-name
   # или
   git checkout -b fix/your-bug-fix
   ```

## Структура проекта

```
screenplay-analyzer/
├── app.py                    # Веб-приложение FastAPI
├── screenplay_processor.py   # Главный процессор
├── document_parser.py        # Парсинг документов
├── scene_segmenter.py        # Сегментация сцен
├── nlp_analyzer.py           # NLP-анализ
├── data_exporter.py          # Экспорт данных
├── config.py                 # Конфигурация
├── templates/                # HTML-шаблоны
│   └── index.html
├── static/                   # Статические файлы
├── uploads/                  # Загруженные файлы
├── outputs/                  # Результаты обработки
└── tests/                    # Тесты (создайте если нужно)
```

## Стиль кода

### Python

Мы следуем [PEP 8](https://pep8.org/) с небольшими дополнениями:

```python
# Используйте type hints
def process_scene(scene: Dict[str, Any]) -> Dict[str, Any]:
    """
    Обрабатывает сцену
    
    Args:
        scene: словарь со сценой
        
    Returns:
        Dict: обработанная сцена
    """
    pass

# Docstrings для всех публичных функций
# Google style или NumPy style

# Максимальная длина строки: 100 символов
# Используйте f-strings для форматирования

# Константы в ВЕРХНЕМ_РЕГИСТРЕ
MAX_FILE_SIZE = 50

# Классы в CamelCase
class SceneAnalyzer:
    pass

# Функции и переменные в snake_case
def analyze_screenplay(file_path: str) -> Dict:
    pass
```

### Форматирование кода

Используйте `black` для автоматического форматирования:

```bash
# Форматировать один файл
black screenplay_processor.py

# Форматировать все файлы
black *.py

# Проверить без изменений
black --check *.py
```

### Проверка стиля

```bash
flake8 *.py --max-line-length=100
```

## Тестирование

### Создание тестов

Создайте тесты в директории `tests/`:

```python
# tests/test_scene_segmenter.py
import pytest
from scene_segmenter import SceneSegmenter

def test_segment_basic():
    segmenter = SceneSegmenter()
    text = """
    1. ИНТ. КУХНЯ - ДЕНЬ
    
    Текст сцены...
    """
    
    scenes = segmenter.segment(text)
    assert len(scenes) == 1
    assert scenes[0]['scene_number'] == 1
```

### Запуск тестов

```bash
pytest tests/
```

## Отправка изменений

### 1. Коммиты

Используйте информативные сообщения:

```bash
# Хорошо ✓
git commit -m "Добавлена поддержка английских сценариев"
git commit -m "Исправлена ошибка парсинга PDF с кириллицей"
git commit -m "Улучшена производительность NLP-анализа на 30%"

# Плохо ✗
git commit -m "fix"
git commit -m "update"
git commit -m "asdf"
```

Формат сообщений:

```
<тип>: <краткое описание>

<детальное описание (опционально)>

<ссылки на issues (опционально)>
```

Типы:
- `feat`: новая функция
- `fix`: исправление бага
- `docs`: документация
- `style`: форматирование
- `refactor`: рефакторинг
- `test`: тесты
- `perf`: оптимизация

### 2. Pull Request

1. Убедитесь, что код отформатирован:
   ```bash
   black *.py
   flake8 *.py --max-line-length=100
   ```

2. Запустите тесты (если есть):
   ```bash
   pytest tests/
   ```

3. Отправьте изменения:
   ```bash
   git push origin feature/your-feature-name
   ```

4. Создайте Pull Request на GitHub

5. В описании PR укажите:
   - Что изменено и почему
   - Как протестировано
   - Скриншоты (для UI изменений)
   - Ссылки на связанные issues

### Шаблон Pull Request

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] Исправление бага
- [ ] Новая функция
- [ ] Улучшение существующей функции
- [ ] Документация
- [ ] Рефакторинг

## Как протестировано
Опишите, как вы тестировали изменения

## Чеклист
- [ ] Код следует стилю проекта
- [ ] Добавлены/обновлены docstrings
- [ ] Добавлены/обновлены тесты
- [ ] Обновлена документация
- [ ] Все тесты проходят
```

## Приоритетные направления

### 🔥 Высокий приоритет

1. **Улучшение точности NLP**
   - Лучшее распознавание персонажей
   - Более точное определение локаций
   - Распознавание специфического реквизита

2. **Поддержка форматов**
   - Улучшенный парсинг сложных PDF
   - Поддержка Final Draft (.fdx)
   - Поддержка Fountain формата

3. **Производительность**
   - Оптимизация обработки больших файлов
   - Кэширование моделей
   - Параллельная обработка сцен

### 🎯 Средний приоритет

4. **Расширение анализа**
   - Определение эмоциональной окраски сцен
   - Анализ диалогов vs действий
   - Определение ритма сценария

5. **UI/UX улучшения**
   - Drag-and-drop для файлов
   - Прогресс-бар с процентами
   - Превью результатов перед экспортом

6. **Интеграция**
   - API для других сервисов
   - Webhook уведомления
   - Экспорт в специализированное ПО

### 💡 Низкий приоритет

7. **Дополнительные функции**
   - Визуализация графиков
   - Сравнение разных версий сценария
   - Автоматические рекомендации

8. **Локализация**
   - Английский интерфейс
   - Другие языки

## Полезные ресурсы

- [PEP 8 Style Guide](https://pep8.org/)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [Natasha Documentation](https://github.com/natasha/natasha)
- [pytest Documentation](https://docs.pytest.org/)

## Вопросы и обсуждения

- Создавайте Issues для обсуждения идей
- Используйте Discussions для вопросов
- Будьте вежливы и конструктивны

## Code Review

Ваш PR будет рассмотрен с учетом:

- **Функциональность**: Работает ли код как задумано?
- **Качество**: Читаем ли код? Есть ли дублирование?
- **Тесты**: Покрыты ли изменения тестами?
- **Документация**: Обновлена ли документация?
- **Производительность**: Не ухудшилась ли производительность?

## Лицензия

Отправляя изменения, вы соглашаетесь, что ваш вклад будет лицензирован под MIT License.

---

**Благодарим за вклад в проект! 🎉**

Ваша помощь делает инструмент лучше для всего киносообщества.

